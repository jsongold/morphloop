// Owned by the textbook resource PR; replace this stub.
//
// Registers nothing yet. When the textbook resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "textbook", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
