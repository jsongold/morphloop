// Owned by the highlight resource PR; replace this stub.
//
// Registers nothing yet. When the highlight resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "highlight", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
