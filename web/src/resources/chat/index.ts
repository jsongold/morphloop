// Owned by the chat resource PR; replace this stub.
//
// Registers nothing yet. When the chat resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "chat", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
