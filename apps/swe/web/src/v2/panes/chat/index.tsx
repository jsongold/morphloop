"use client";

// Owned by the chat pane PR; replace this directory. Side slot: the ws's main
// thread, or the active thread (`threadId` in src/v2/state.tsx).

export default function ChatPane() {
  return <p className="muted">chat pane: not implemented</p>;
}
