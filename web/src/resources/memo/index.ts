// Owned by the memo resource PR; replace this stub.
//
// Registers nothing yet. When the memo resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "memo", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
