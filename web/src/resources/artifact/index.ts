// Owned by the artifact resource PR; replace this stub.
//
// Registers nothing yet. When the artifact resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "artifact", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
