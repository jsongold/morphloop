{
  "id": "gen-diagnose-dns-resolver-misconfiguration-001",
  "question": "**ledger.corp.internal is healthy, but this host can't reach it by name**\n\nThe `ledger.corp.internal` service is reported healthy, but requests from this host fail when you use its name.\n\nYour job:\n1. Determine whether the failure is in the web service, DNS, or this host's name-resolution path.\n2. Fix this host so `ledger.corp.internal` resolves through DNS again and the service's HTTP health endpoint can be reached by name.\n\nDo not work around the problem by pinning an address locally; restore normal DNS-based resolution.",
  "expected": "All checks pass:\n- `dns.resolver_answers`: the configured resolver answers `ledger.corp.internal` A `127.0.0.42`.\n- `dns.name_resolves`: `ledger.corp.internal` resolves to `127.0.0.42`.\n- `dns.http_status`: `http://ledger.corp.internal:9093/readyz` returns HTTP 200.",
  "answer_mode": "artifact",
  "artifact_ref": "diagnose-dns-resolver-misconfiguration-lab",
  "labels": [
    "topic:network.dns.resolution",
    "activity:diagnose",
    "troubleshooting",
    "dimension:explain",
    "dimension:observe",
    "dimension:diagnose",
    "dimension:repair"
  ]
}
