{
  "description": "External media carry URI + content hash (ADR-0015).",
  "schema": "schemas/pack/reference.json",
  "expected_error_paths": [
    "$.media[0]"
  ],
  "instance": {
    "id": "network.dns.resolver",
    "version": "1",
    "title": "DNS resolver",
    "summary": "The component that turns a name into an address by asking DNS servers.",
    "aliases": [
      "resolver",
      "nameserver",
      "resolv.conf"
    ],
    "skills": [
      "network.dns.resolution"
    ],
    "sections": [
      {
        "kind": "what",
        "body": "A client library plus configured servers that answer name lookups."
      },
      {
        "kind": "observe",
        "body": "`cat /etc/resolv.conf`, `dig name`, `getent hosts name`."
      },
      {
        "kind": "failure_modes",
        "body": "Wrong nameserver, missing search domain, unreachable port 53."
      }
    ],
    "media": [
      {
        "uri": "https://assets.morphloop.dev/dns/resolver-path.svg",
        "media_type": "image/svg+xml",
        "alt": "Query path from application through stub and recursive resolvers."
      }
    ]
  }
}
