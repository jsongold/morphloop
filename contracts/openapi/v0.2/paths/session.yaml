# URL -> Path Item map for the session resource (ADR-0018). Owned by the
# session resource PR: this file is the only file that PR edits (plus an
# optional new components/session.yaml for session-specific schemas).
# Never edit ../root.yaml or another resource's paths/*.yaml — new URLs are
# just new top-level keys in this file, merged into the document by
# harness.testing.openapi_v2.load_merged_openapi_v2_spec (duplicate URLs
# across files are a load-time error). See ../README.md.
#
# Session: topic, tree, sittings. Selecting a topic copies the pack's topic subtree and pins pack_hash; a sitting is derived from event timestamps.
#
# `/session/_stub` below is a placeholder with no meaning; replace or
# extend it with the resource's real paths.
/session/_stub:
  get:
    tags: [session]
    operationId: getSessionStub
    summary: Placeholder for the session resource.
    description: |
      No behavior yet. Real endpoints for session land in a follow-up PR
      that edits only this file (ADR-0018, issue #34).
    responses:
      '200':
        description: Stub response.
        content:
          application/json:
            schema:
              $ref: '../components/common.yaml#/Stub'
      default:
        $ref: '../root.yaml#/components/responses/Problem'
