# URL -> Path Item map for the events resource (ADR-0018). Owned by the
# events resource PR: this file is the only file that PR edits (plus an
# optional new components/events.yaml for events-specific schemas).
# Never edit ../root.yaml or another resource's paths/*.yaml — new URLs are
# just new top-level keys in this file, merged into the document by
# harness.testing.openapi_v2.load_merged_openapi_v2_spec (duplicate URLs
# across files are a load-time error). See ../README.md.
#
# Events: event. Minimal envelope (id, position, type, created_at, actor, user_id, session_id?, ws_id?, payload). Replaces the v0.1 event envelope for v0.2 (issue #43).
#
# `/events/_stub` below is a placeholder with no meaning; replace or
# extend it with the resource's real paths.
/events/_stub:
  get:
    tags: [events]
    operationId: getEventsStub
    summary: Placeholder for the events resource.
    description: |
      No behavior yet. Real endpoints for events land in a follow-up PR
      that edits only this file (ADR-0018, issue #34).
    responses:
      '200':
        description: Stub response.
        content:
          application/json:
            schema:
              $ref: '../components/common.yaml#/Stub'
      default:
        $ref: '../root.yaml#/components/responses/Problem'
