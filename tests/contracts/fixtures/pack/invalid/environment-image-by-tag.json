{
  "description": "Lab images are pinned by digest, not tag (ADR-0010, ADR-0015).",
  "schema": "schemas/pack/environment.json",
  "expected_error_paths": [
    "$.image.repository",
    "$.image.digest"
  ],
  "instance": {
    "id": "dns-broken-resolver-v1",
    "fixture": "dns.broken-resolver",
    "image": {
      "repository": "ghcr.io/morphloop/dns-lab:latest",
      "digest": "latest"
    },
    "params": {
      "fault": "wrong_nameserver",
      "service_name": "api.internal"
    }
  }
}
