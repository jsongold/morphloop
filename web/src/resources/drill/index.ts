// Owned by the drill resource PR; replace this stub.
//
// Registers nothing yet. When the drill resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "drill", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
