{
  "id": "dns-resolver-config-file",
  "question": "Which file tells the stub resolver which nameservers to send its queries to?",
  "expected": "`/etc/resolv.conf`: it lists up to three `nameserver` addresses, optional `search` domains and `options` such as `timeout:` and `attempts:`.",
  "answer_mode": "text",
  "labels": [
    "topic:network.dns.resolver",
    "dimension:explain"
  ]
}
