{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://morphloop.dev/contracts/schemas/websocket/payloads/terminal.exit.json",
  "title": "terminal.exit payload",
  "description": "The PTY process for this connection's terminal ended. v0.1 has no corresponding learning event type (EVENT_SCHEMA.md lists 'terminal.exit' as a future event, not yet registered in contracts/schemas/events/dispatch.json), so this is a WebSocket-only signal for now and carries no idempotency_key.",
  "type": "object",
  "required": [
    "exit_code",
    "signal"
  ],
  "properties": {
    "exit_code": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "signal": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false
}
