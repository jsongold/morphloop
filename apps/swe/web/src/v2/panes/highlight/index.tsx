"use client";

// Owned by the highlight pane PR; replace this directory. Main slot, rendered
// after the reader: the selection toolbar and popup chat anchored to a block
// (`[data-block-id]`, src/v2/panes/types.ts).

export default function HighlightPane() {
  return <p className="muted">highlight pane: not implemented</p>;
}
