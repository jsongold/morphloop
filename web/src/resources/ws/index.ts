// Owned by the ws resource PR; replace this stub.
//
// Registers nothing yet. When the ws resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "ws", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
