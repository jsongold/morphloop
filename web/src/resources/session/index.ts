// Owned by the session resource PR; replace this stub.
//
// Registers nothing yet. When the session resource lands, this file imports
// { registerResource } from "../index" and calls it at module scope
// (registerResource({ id: "session", label: ..., View: ... })); it is already
// imported from "../index.ts" for that side effect.
export {};
