# URL -> Path Item map for the highlight resource (ADR-0018). Owned by the
# highlight resource PR: this file is the only file that PR edits (plus an
# optional new components/highlight.yaml for highlight-specific schemas).
# Never edit ../root.yaml or another resource's paths/*.yaml — new URLs are
# just new top-level keys in this file, merged into the document by
# harness.testing.openapi_v2.load_merged_openapi_v2_spec (duplicate URLs
# across files are a load-time error). See ../README.md.
#
# Highlight: items (a View). A learner's free-form highlight is the event `highlight.created`; anchor is doc_id/block_id + a W3C TextQuoteSelector and TextPositionSelector.
#
# `/highlight/_stub` below is a placeholder with no meaning; replace or
# extend it with the resource's real paths.
/highlight/_stub:
  get:
    tags: [highlight]
    operationId: getHighlightStub
    summary: Placeholder for the highlight resource.
    description: |
      No behavior yet. Real endpoints for highlight land in a follow-up PR
      that edits only this file (ADR-0018, issue #34).
    responses:
      '200':
        description: Stub response.
        content:
          application/json:
            schema:
              $ref: '../components/common.yaml#/Stub'
      default:
        $ref: '../root.yaml#/components/responses/Problem'
